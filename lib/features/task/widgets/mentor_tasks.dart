

import 'package:flutter/material.dart';

class MentorTasksWidget extends StatelessWidget {
  const MentorTasksWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Placeholder(),
      ],
    );
  }
}