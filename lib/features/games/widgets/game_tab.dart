import 'package:flutter/material.dart';

class GameTabWidget extends StatelessWidget {
  const GameTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}